import React from 'react'

const BlogPage = () => {
  return (
    <div>
      {/* {blogPosts.map((post, index) => (
        <div key={index}>
          <h2>{post.title}</h2>
          <p>{post.content}</p>
        </div>
      ))} */}
    </div>
  )
}

export default BlogPage

